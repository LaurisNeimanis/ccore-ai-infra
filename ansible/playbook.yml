# =====================================================================
# Application Server — modular role-based deployment playbook
# =====================================================================

- name: Configure application server
  hosts: app
  become: true
  any_errors_fatal: true

  roles:
    - docker
    - app_deploy
